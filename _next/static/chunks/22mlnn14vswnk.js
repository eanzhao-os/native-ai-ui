(globalThis.TURBOPACK||(globalThis.TURBOPACK=[])).push(["object"==typeof document?document.currentScript:void 0,6455,e=>{"use strict";var t=e.i(43476),a=e.i(71645),r=e.i(93633),o=e.i(63125);let i=(0,r.accentChain)([r.ACCENTS.red,r.ACCENTS.orange,r.ACCENTS.yellow,r.ACCENTS.green,r.ACCENTS.cyan,r.ACCENTS.blue,r.ACCENTS.purple]);function n({children:e,size:a=15,strokeWidth:r=1.8}){return(0,t.jsx)("svg",{width:a,height:a,viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:r,strokeLinecap:"round",strokeLinejoin:"round","aria-hidden":"true",children:e})}let s={clip:(0,t.jsx)("path",{d:"m21.4 11.05-9.19 9.19a6 6 0 0 1-8.49-8.49l8.57-8.57A4 4 0 1 1 18 8.84l-8.59 8.57a2 2 0 0 1-2.83-2.83l8.49-8.48"}),chart:(0,t.jsx)("path",{d:"M4 20V10M10 20V4M16 20v-7M22 20H2"}),layers:(0,t.jsxs)("g",{children:[(0,t.jsx)("path",{d:"M12 2 2 7l10 5 10-5-10-5z"}),(0,t.jsx)("path",{d:"M2 17l10 5 10-5M2 12l10 5 10-5"})]}),globe:(0,t.jsxs)("g",{children:[(0,t.jsx)("circle",{cx:"12",cy:"12",r:"10"}),(0,t.jsx)("path",{d:"M2 12h20M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"})]})},l={figma:(0,t.jsxs)("svg",{width:"11",height:"16",viewBox:"0 0 38 57","aria-hidden":"true",children:[(0,t.jsx)("path",{d:"M9.5 57A9.5 9.5 0 0 0 19 47.5V38H9.5a9.5 9.5 0 0 0 0 19z",fill:"#0ACF83"}),(0,t.jsx)("path",{d:"M0 28.5A9.5 9.5 0 0 1 9.5 19H19v19H9.5A9.5 9.5 0 0 1 0 28.5z",fill:"#A259FF"}),(0,t.jsx)("path",{d:"M0 9.5A9.5 9.5 0 0 1 9.5 0H19v19H9.5A9.5 9.5 0 0 1 0 9.5z",fill:"#F24E1E"}),(0,t.jsx)("path",{d:"M19 0h9.5a9.5 9.5 0 1 1 0 19H19V0z",fill:"#FF7262"}),(0,t.jsx)("path",{d:"M38 28.5a9.5 9.5 0 1 1-19 0 9.5 9.5 0 0 1 19 0z",fill:"#1ABCFE"})]}),slack:(0,t.jsxs)("svg",{width:"15",height:"15",viewBox:"0 0 127 127","aria-hidden":"true",children:[(0,t.jsx)("path",{d:"M27.2 80c0 7.3-5.9 13.2-13.2 13.2C6.7 93.2.8 87.3.8 80c0-7.3 5.9-13.2 13.2-13.2h13.2V80zm6.6 0c0-7.3 5.9-13.2 13.2-13.2 7.3 0 13.2 5.9 13.2 13.2v33c0 7.3-5.9 13.2-13.2 13.2-7.3 0-13.2-5.9-13.2-13.2V80z",fill:"#E01E5A"}),(0,t.jsx)("path",{d:"M47 27.2c-7.3 0-13.2-5.9-13.2-13.2C33.8 6.7 39.7.8 47 .8c7.3 0 13.2 5.9 13.2 13.2v13.2H47zm0 6.7c7.3 0 13.2 5.9 13.2 13.2 0 7.3-5.9 13.2-13.2 13.2H13.9C6.6 60.3.7 54.4.7 47.1c0-7.3 5.9-13.2 13.2-13.2H47z",fill:"#36C5F0"}),(0,t.jsx)("path",{d:"M99.9 47.1c0-7.3 5.9-13.2 13.2-13.2 7.3 0 13.2 5.9 13.2 13.2 0 7.3-5.9 13.2-13.2 13.2H99.9V47.1zm-6.6 0c0 7.3-5.9 13.2-13.2 13.2-7.3 0-13.2-5.9-13.2-13.2V13.9C66.9 6.6 72.8.7 80.1.7c7.3 0 13.2 5.9 13.2 13.2v33.2z",fill:"#2EB67D"}),(0,t.jsx)("path",{d:"M80.1 99.8c7.3 0 13.2 5.9 13.2 13.2 0 7.3-5.9 13.2-13.2 13.2-7.3 0-13.2-5.9-13.2-13.2V99.8h13.2zm0-6.6c-7.3 0-13.2-5.9-13.2-13.2 0-7.3 5.9-13.2 13.2-13.2h33.1c7.3 0 13.2 5.9 13.2 13.2 0 7.3-5.9 13.2-13.2 13.2H80.1z",fill:"#ECB22E"})]}),gmail:(0,t.jsxs)("svg",{width:"15",height:"12",viewBox:"0 0 256 193","aria-hidden":"true",children:[(0,t.jsx)("path",{d:"M58.182 192.05V93.14L27.507 65.077 0 49.504v125.091c0 9.658 7.825 17.455 17.455 17.455h40.727Z",fill:"#4285F4"}),(0,t.jsx)("path",{d:"M197.818 192.05h40.727c9.659 0 17.455-7.826 17.455-17.455V49.505l-31.156 17.837-27.026 25.798v98.91Z",fill:"#34A853"}),(0,t.jsx)("path",{d:"m58.182 93.14-4.174-38.647 4.174-36.989L128 69.868l69.818-52.364 4.669 34.992-4.669 40.644L128 145.504 58.182 93.14Z",fill:"#EA4335"}),(0,t.jsx)("path",{d:"M197.818 17.504V93.14L256 49.504V26.231c0-21.585-24.64-33.89-41.89-20.945l-16.292 12.218Z",fill:"#FBBC04"}),(0,t.jsx)("path",{d:"m0 49.504 26.759 20.07L58.182 93.14V17.504L41.89 5.286C24.61-7.66 0 4.646 0 26.23v23.273Z",fill:"#C5221F"})]})},u=[{key:"attach",nameEn:"Add photos & files",nameZh:"添加图片和文件",descEn:"Upload from your computer",descZh:"从本地上传",glyph:"clip",attach:!0},{key:"scoop",nameEn:"Scoop Data",nameZh:"Scoop 数据",descEn:"Sales & churn metrics",descZh:"销售与产量指标",glyph:"chart"},{key:"flavors",nameEn:"Flavor records",nameZh:"风味档案",descEn:"26 makers, tags, links",descZh:"26 家厂商、标签与链接",glyph:"layers"},{key:"web",nameEn:"Web search",nameZh:"联网搜索",descEn:"Real-time news and info",descZh:"实时新闻与资讯",glyph:"globe"},{key:"figma",nameEn:"Figma",nameZh:"Figma",descEn:"Design-to-code workflows",descZh:"设计稿转代码工作流",brand:"figma"},{key:"slack",nameEn:"Slack",nameZh:"Slack",descEn:"Read and manage Slack",descZh:"读取并管理 Slack 消息",brand:"slack"},{key:"gmail",nameEn:"Gmail",nameZh:"Gmail",descEn:"Read and manage Gmail",descZh:"读取并管理 Gmail 邮件",brand:"gmail",connect:!0}],c=[{key:"compare",name:"/compare",descEn:"Flavor vs. last summer",descZh:"对比风味与去年同期销量"},{key:"churn-plan",name:"/churn-plan",descEn:"Draft a churn schedule",descZh:"起草搅拌生产排期"},{key:"restock",name:"/restock",descEn:"Build a reorder list",descZh:"生成补货清单"},{key:"draft-email",name:"/draft-email",descEn:"Write a supplier email",descZh:"撰写供应商邮件"},{key:"summarize",name:"/summarize",descEn:"Digest the thread so far",descZh:"总结当前对话要点"}],d=[{key:"sprinkles-5",name:"Sprinkles 5",tagEn:"Flagship",tagZh:"旗舰"},{key:"vanilla-1",name:"Vanilla 1",tagEn:"Basic",tagZh:"基础"},{key:"freezer-burn",name:"Freezer Burn 0.4",tagEn:"Stale",tagZh:"过时"}],h=["flavor-chart.png","summer-menu.pdf","pos-export.csv"],f=[{draft:"",connect:!1,model:"vanilla-1",hold:1100},{draft:"@",active:0,hold:900},{draft:"@",active:1,hold:620},{draft:"@",active:4,hold:620},{draft:"@",active:6,hold:700},{draft:"@",active:6,connect:!0,hold:1e3},{draft:"",hold:700},{draft:"/",active:0,hold:900},{draft:"/",active:1,hold:620},{draft:"/",active:3,hold:1e3},{draft:"",hold:800},{draft:"",modelOpen:!0,hold:1200},{draft:"",model:"sprinkles-5",hold:2400},{draft:"",hold:900}];e.s(["default",0,function({variant:e="Rounded",lang:p}){let m,g="zh"===(0,o.useLang)("prompt-bar",p),v="Pill"===e,[x,b]=(0,a.useState)(""),[w,A]=(0,a.useState)(!1),[y,T]=(0,a.useState)(!1),[E,S]=(0,a.useState)(!1),[P,R]=(0,a.useState)(d[1]),[U,k]=(0,a.useState)([]),[L,M]=(0,a.useState)(!1),[_,F]=(0,a.useState)(0),[B,C]=(0,a.useState)(!1),[D,N]=(0,a.useState)(!0),[I,V]=(0,a.useState)(0),[j,z]=(0,a.useState)(!1),[O,H]=(0,a.useState)(null),[W,G]=(0,a.useState)(!1),[X,Z]=(0,a.useState)(null),[$,Y]=(0,a.useState)(null),q=(0,a.useRef)(null),K=(0,a.useRef)(null),Q=(0,a.useRef)(null),J=(0,a.useRef)(null),ee=(0,a.useRef)([]),et=(0,a.useRef)([]),ea=(0,a.useRef)(null),er=(0,a.useRef)(null),eo=(0,a.useRef)(!1),ei=e=>{N(!1),D&&e.target===K.current&&b("")},en=w?null:(m=/(^|\s)([@/])([\w-]*)$/.exec(x))?{kind:"@"===m[2]?"at":"slash",query:m[3].toLowerCase(),start:m.index+m[1].length}:null,es=y?"at":en?.kind??null,el=y?"":en?.query??"",eu="at"===es?u.filter(e=>(g?e.nameZh:e.nameEn).toLowerCase().includes(el)).map(e=>({key:e.key,name:g?e.nameZh:e.nameEn,desc:g?e.descZh:e.descEn})):"slash"===es?c.filter(e=>e.name.slice(1).startsWith(el)).map(e=>({key:e.key,name:e.name,desc:g?e.descZh:e.descEn})):[];(0,a.useEffect)(()=>{F(0),G(!1)},[es,el]),(0,a.useLayoutEffect)(()=>{let e=ee.current[_];e&&H({top:e.offsetTop,height:e.offsetHeight})},[es,el,_,L,eu.length]);let ec=d.findIndex(e=>e.key===P.key);(0,a.useLayoutEffect)(()=>{if(!E)return;let e=et.current[$??ec];e&&Z({top:e.offsetTop,height:e.offsetHeight})},[E,$,ec]),(0,a.useEffect)(()=>{E||Y(null)},[E]);let ed=()=>{let e=ea.current;return e?(0,r.createShader)({canvas:e,palette:i,direction:"ltr",bandTight:10,swellAmount:.85}):null};(0,a.useEffect)(()=>(er.current=ed(),()=>{er.current?.destroy(),er.current=null}),[]);let eh=e=>{R(e),S(!1),"sprinkles-5"===e.key&&(()=>{if(eo.current||window.matchMedia("(prefers-reduced-motion: reduce)").matches)return;er.current?.destroy();let e=ed();er.current=e,e&&(eo.current=!0,(0,r.playSweep)(e,{palette:i,direction:"ltr",sweepMs:950,outroMs:130,peakAlpha:1.3,bandTight:10,brightness:1.4,swellAmount:1,waveSpeed:1.3,easing:"easeOutExpo"}).done.finally(()=>{eo.current=!1}))})()};(0,a.useEffect)(()=>{if(!D)return;let e=f[I%f.length];if(b(e.draft),void 0!==e.active&&F(e.active),void 0!==e.connect&&M(e.connect),void 0!==e.modelOpen&&S(e.modelOpen),e.model){let t=d.find(t=>t.key===e.model);t&&eh(t)}let t=setTimeout(()=>V(e=>e+1),e.hold);return()=>clearTimeout(t)},[D,I]),(0,a.useEffect)(()=>{if(!B)return;let e=g?"对比开心果口味周末销量与去年同期":"Compare pistachio weekends to last summer",t=setTimeout(()=>{b(t=>t?`${t.trimEnd()} ${e}`:e),C(!1),K.current?.focus()},2200);return()=>clearTimeout(t)},[B,g]),(0,a.useLayoutEffect)(()=>{let e=K.current,t=q.current,a=Q.current,r=J.current;if(!e||!t||!a||!r)return;let o=84+r.offsetWidth,i=t.clientWidth-o-16,n=x.includes("\n")||a.offsetWidth+8>i;n!==j&&z(n),e.style.height="0px";let s=e.scrollHeight;e.style.height=`${Math.min(Math.max(s,28),100)}px`,e.style.overflowY=s>100?"auto":"hidden"},[x,j]);let ef=()=>{T(!1),S(!1)},ep=e=>{let t=u.find(t=>t.key===e.key);t?.attach?(k(e=>[...e,h[e.length%h.length]]),en&&b(x.slice(0,en.start))):"at"===es?b(`${en?x.slice(0,en.start):x}@${e.name} `):b(`${en?x.slice(0,en.start):x}${e.name} `),T(!1),A(!1),K.current?.focus()},em=x.trim().length>0||U.length>0,eg=()=>{em&&(b(""),k([]),ef())};return(0,t.jsx)("div",{className:"flex min-h-[384px] w-full max-w-105 flex-col justify-end pb-8",onPointerDownCapture:ei,onKeyDownCapture:ei,children:(0,t.jsxs)("div",{className:"relative",children:[es&&(0,t.jsxs)("div",{onMouseLeave:()=>G(!1),className:"absolute inset-x-0 bottom-full z-10 mb-2 rounded-card bg-surface p-1 shadow-raised",style:{animation:"pop-in 180ms cubic-bezier(0.23,1,0.32,1) both",transformOrigin:"bottom center"},children:[(0,t.jsx)("span",{"aria-hidden":!0,className:"pointer-events-none absolute inset-x-1 rounded-[6px] bg-hover",style:{top:O?.top??0,height:O?.height??0,opacity:O&&W&&eu.length>0?1:0,transition:"top 220ms cubic-bezier(0.23,1,0.32,1), height 220ms cubic-bezier(0.23,1,0.32,1), opacity 150ms ease"}}),eu.map((e,a)=>{let r="at"===es?u.find(t=>t.key===e.key):void 0;return(0,t.jsxs)("button",{type:"button",ref:e=>{ee.current[a]=e},onMouseDown:e=>e.preventDefault(),onMouseEnter:()=>{F(a),G(!0)},onClick:()=>ep(e),className:"relative z-10 flex h-9 w-full items-center gap-2.5 rounded-[6px] px-2 text-left",children:[r&&(0,t.jsx)("span",{className:"flex size-5.5 shrink-0 items-center justify-center text-ink-2",children:r.brand?l[r.brand]:(0,t.jsx)(n,{size:15,children:s[r.glyph??"clip"]})}),(0,t.jsx)("span",{className:"shrink-0 text-[12.5px] font-medium text-ink",children:e.name}),(0,t.jsx)("span",{className:"min-w-0 flex-1 truncate text-[12px] text-ink-3",children:e.desc}),r?.connect&&(0,t.jsx)("span",{role:"button",tabIndex:-1,onClick:e=>{e.stopPropagation(),M(e=>!e)},className:`shrink-0 text-[12px] font-medium transition-colors duration-100 ${L?"text-green":"text-accent-ink hover:underline"}`,children:L?g?"已连接":"Connected":g?"连接":"Connect"})]},e.key)}),0===eu.length&&(0,t.jsx)("div",{className:"flex h-9 items-center px-2 text-[12px] text-ink-3",children:g?`没有匹配「${el}」的结果`:`No matches for “${el}”`}),(0,t.jsx)("div",{className:"mt-1 border-t border-line px-2 pt-1.5 pb-1 text-[11px] text-ink-3",children:"at"===es?g?"输入以搜索数据源与文件":"Type to search sources & files":g?"输入以搜索命令":"Type to search commands"})]}),E&&(0,t.jsxs)("div",{onMouseLeave:()=>Y(null),className:"absolute right-0 bottom-full z-10 mb-2 w-44 rounded-card bg-surface p-1 shadow-raised",style:{animation:"pop-in 180ms cubic-bezier(0.23,1,0.32,1) both",transformOrigin:"bottom right"},children:[(0,t.jsx)("span",{"aria-hidden":!0,className:"pointer-events-none absolute inset-x-1 rounded-[6px] bg-hover",style:{top:X?.top??0,height:X?.height??0,opacity:X&&null!==$?1:0,transition:"top 220ms cubic-bezier(0.23,1,0.32,1), height 220ms cubic-bezier(0.23,1,0.32,1), opacity 150ms ease"}}),d.map((e,a)=>(0,t.jsxs)("button",{type:"button",ref:e=>{et.current[a]=e},onMouseDown:e=>e.preventDefault(),onMouseEnter:()=>Y(a),onClick:()=>{eh(e),K.current?.focus()},className:"relative z-10 flex h-7.5 w-full items-center gap-2 rounded-[6px] px-2 text-left",children:[(0,t.jsx)("span",{className:"min-w-0 flex-1 truncate text-[12.5px] font-medium text-ink",children:e.name}),(0,t.jsx)("span",{className:"shrink-0 text-[11px] text-ink-3",children:g?e.tagZh:e.tagEn}),(0,t.jsx)("span",{className:`shrink-0 text-ink ${e.key===P.key?"":"invisible"}`,children:(0,t.jsx)(n,{size:13,strokeWidth:2.5,children:(0,t.jsx)("path",{d:"M20 6L9 17l-5-5"})})})]},e.key))]}),(0,t.jsxs)("div",{className:`relative isolate flex flex-col gap-1.5 overflow-hidden border border-line bg-surface p-1.5 shadow-card transition-[border-color,border-radius] duration-150 focus-within:border-line-strong ${v?U.length>0||j?"rounded-[24px]":"rounded-full":"rounded-[14px]"}`,children:[(0,t.jsx)("canvas",{ref:ea,"aria-hidden":"true",className:"pointer-events-none absolute inset-0 -z-10 h-full w-full",style:{borderRadius:"inherit"}}),(0,t.jsx)("span",{ref:Q,"aria-hidden":"true",className:"pointer-events-none absolute invisible whitespace-pre text-[13px] leading-[18px]",children:x}),U.length>0&&(0,t.jsx)("div",{className:`flex flex-wrap gap-1.5 pt-0.5 ${v?"px-1":"px-0.5"}`,children:U.map((e,a)=>(0,t.jsxs)("span",{className:`flex h-6.5 items-center gap-1.5 bg-field py-1 pr-1 pl-1.5 text-[11.5px] text-ink-2 shadow-hairline ${v?"rounded-full":"rounded-chip"}`,style:{animation:"pop-in 200ms cubic-bezier(0.23,1,0.32,1) both"},children:[(0,t.jsx)(n,{size:12,children:(0,t.jsxs)("g",{children:[(0,t.jsx)("path",{d:"M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"}),(0,t.jsx)("path",{d:"M14 2v6h6"})]})}),(0,t.jsx)("span",{className:"max-w-36 truncate",children:e}),(0,t.jsx)("button",{type:"button","aria-label":g?`移除 ${e}`:`Remove ${e}`,onClick:()=>k(e=>e.filter((e,t)=>t!==a)),className:`flex size-4 items-center justify-center text-ink-3 transition-colors duration-100 hover:bg-line/70 hover:text-ink ${v?"rounded-full":"rounded-[4px]"}`,children:(0,t.jsx)(n,{size:10,strokeWidth:2.5,children:(0,t.jsx)("path",{d:"M18 6L6 18M6 6l12 12"})})})]},`${e}-${a}`))}),(0,t.jsxs)("div",{ref:q,className:`grid items-end gap-x-1 gap-y-1.5 ${j?"grid-cols-[minmax(0,1fr)_auto_28px_28px]":"grid-cols-[28px_minmax(0,1fr)_auto_28px_28px]"}`,children:[(0,t.jsx)("button",{type:"button","aria-label":g?"添加附件与数据源":"Add attachments and sources","aria-expanded":y,onClick:()=>{S(!1),T(e=>!e),K.current?.focus()},className:`flex size-7 shrink-0 items-center justify-center justify-self-start text-ink-3 transition-[background-color,color,transform] duration-150 hover:bg-hover hover:text-ink active:scale-[0.94] ${v?"rounded-full":"rounded-[8px]"} ${y?"bg-hover text-ink":""} ${j?"col-start-1 row-start-2":"col-start-1 row-start-1"}`,children:(0,t.jsx)(n,{size:16,strokeWidth:2,children:(0,t.jsx)("path",{d:"M12 5v14M5 12h14"})})}),(0,t.jsx)("textarea",{ref:K,rows:1,value:x,onChange:e=>{b(e.target.value),A(!1),T(!1)},onKeyDown:e=>{if(es&&eu.length>0){if("ArrowDown"===e.key||"ArrowUp"===e.key){e.preventDefault(),G(!0),F(t=>(t+("ArrowDown"===e.key?1:eu.length-1))%eu.length);return}if("Enter"===e.key&&!e.shiftKey||"Tab"===e.key){e.preventDefault(),ep(eu[_]);return}}if("Escape"===e.key){A(!0),ef();return}"Enter"!==e.key||e.shiftKey||e.nativeEvent.isComposing||(e.preventDefault(),eg())},placeholder:B?g?"正在聆听…":"Listening…":g?"输入消息…":"Write a message…","aria-label":g?"提示词输入框":"Prompt",className:`min-h-7 min-w-0 w-full resize-none bg-transparent px-1 py-[5px] text-[13px] leading-[18px] text-ink outline-none [overflow-wrap:anywhere] placeholder:text-ink-3 ${j?"col-span-full col-start-1 row-start-1":"col-start-2 row-start-1"}`}),(0,t.jsxs)("button",{ref:J,type:"button","aria-expanded":E,"aria-label":g?"选择模型":"Choose model",onClick:()=>{T(!1),S(e=>!e)},className:`flex h-7 shrink-0 items-center gap-1 px-1.5 text-[12px] font-medium text-ink-2 transition-colors duration-150 hover:bg-hover hover:text-ink ${v?"rounded-full":"rounded-[8px]"} ${j?"col-start-2 row-start-2":"col-start-3 row-start-1"}`,children:[P.name,(0,t.jsx)("span",{className:"text-ink-3",children:(0,t.jsx)(n,{size:11,strokeWidth:2.4,children:(0,t.jsx)("path",{d:"M6 9l6 6 6-6"})})})]}),(0,t.jsx)("button",{type:"button","aria-label":B?g?"停止听写":"Stop dictation":g?"开始听写":"Start dictation","aria-pressed":B,onClick:()=>C(e=>!e),className:`flex size-7 shrink-0 items-center justify-center transition-[background-color,color,transform] duration-150 active:scale-[0.94] ${v?"rounded-full":"rounded-[8px]"} ${B?"bg-accent-tint text-accent-ink":"text-ink-3 hover:bg-hover hover:text-ink"} ${j?"col-start-3 row-start-2":"col-start-4 row-start-1"}`,children:B?(0,t.jsx)("span",{className:"flex h-3.5 items-center gap-[2.5px]",children:[0,1,2].map(e=>(0,t.jsx)("span",{className:"w-[2.5px] rounded-full bg-current",style:{height:"100%",animation:`eq-bounce 900ms ease-in-out ${150*e}ms infinite`}},e))}):(0,t.jsx)(n,{size:15,strokeWidth:2,children:(0,t.jsxs)("g",{children:[(0,t.jsx)("path",{d:"M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3z"}),(0,t.jsx)("path",{d:"M19 10v2a7 7 0 0 1-14 0v-2M12 19v3"})]})})}),(0,t.jsx)("button",{type:"button","aria-label":g?"发送":"Send",disabled:!em,onClick:eg,className:`flex size-7 shrink-0 items-center justify-center transition-[background-color,color,transform] duration-200 enabled:active:scale-[0.94] ${v?"rounded-full":"rounded-[8px]"} ${j?"col-start-4 row-start-2":"col-start-5 row-start-1"}`,style:{background:em?"var(--ink)":"var(--line-strong)",color:em?"var(--surface)":"var(--ink-2)"},children:(0,t.jsx)(n,{size:16,strokeWidth:2.4,children:(0,t.jsx)("path",{d:"M12 19V5M5 12l7-7 7 7"})})})]})]})]})})}])},68976,function(e){e.n(e.i(6455))},93633,e=>{"use strict";var t=e=>e<=.04045?e/12.92:Math.pow((e+.055)/1.055,2.4),a=e=>e<=.0031308?12.92*e:1.055*Math.pow(e,1/2.4)-.055,r=e=>{var a,r,o;let i,n,s,[l,u,c]=(a=t(e[0]),i=Math.cbrt(.4122214708*a+.5363325363*(r=t(e[1]))+.0514459929*(o=t(e[2]))),[.2104542553*i+.793617785*(n=Math.cbrt(.2119034982*a+.6806995451*r+.1073969566*o))-.0040720468*(s=Math.cbrt(.0883024619*a+.2817188376*r+.6299787005*o)),1.9779984951*i-2.428592205*n+.4505937099*s,.0259040371*i+.7827717662*n-.808675766*s]);return{L:l,C:Math.sqrt(u*u+c*c),H:Math.atan2(c,u)}},o=({L:e,C:t,H:r})=>{let o,i,n,s,l,u,c=t*Math.cos(r),d=t*Math.sin(r),[h,f,p]=(o=e+.3963377774*c+.2158037573*d,i=e-.1055613458*c-.0638541728*d,n=e-.0894841775*c-1.291485548*d,[4.0767416621*(s=o*o*o)-3.3077115913*(l=i*i*i)+.2309699292*(u=n*n*n),-1.2684380046*s+2.6097574011*l-.3413193965*u,-.0041960863*s-.7034186147*l+1.707614701*u]),m=e=>Math.max(0,Math.min(1,e));return[m(a(h)),m(a(f)),m(a(p))]},i=e=>{let t=parseInt(e.replace("#",""),16);return[(t>>16&255)/255,(t>>8&255)/255,(255&t)/255]},n=(e,t,a)=>{let r,o=e.C<1e-4?t.H:e.H,i=t.C<1e-4?e.H:t.H;return{L:e.L+(t.L-e.L)*a,C:e.C+(t.C-e.C)*a,H:((r=i-o)>Math.PI&&(r-=2*Math.PI),r<-Math.PI&&(r+=2*Math.PI),o+r*a)}};function s(e,t){if(0===e.length)return[];if(1===e.length){let a=o(e[0]);return Array.from({length:t},()=>a)}let a=[],r=e.length;for(let i=0;i<t;i++){let s=i/(t-1)*(r-1),l=Math.min(r-2,Math.floor(s)),u=s-l;a.push(o(n(e[l],e[l+1],u)))}return a}var l={red:"#FF3D7F",orange:"#FF7A1A",yellow:"#FFD600",green:"#C2FF3D",mint:"#00FFA8",teal:"#00E5D6",cyan:"#1FC8FF",blue:"#2E70FF",indigo:"#7B4FFF",purple:"#D33CFF",pink:"#FF3DC0",brown:"#D8A87B"},u=["red","orange","yellow","green","mint","teal","cyan","blue","indigo","purple","pink","brown"],c=Object.fromEntries(Object.entries(l).map(([e,t])=>[e,r(i(t))]));function d(e){let t=e.length,a=e.map((e,a)=>a/(t-1)),r=a.map(e=>Math.cos(2*Math.PI*.5*e)),o=a.map(e=>Math.sin(2*Math.PI*.5*e)),i=r.reduce((e,t)=>e+t,0),n=o.reduce((e,t)=>e+t,0),s=r.reduce((e,t)=>e+t*t,0),l=o.reduce((e,t)=>e+t*t,0),u=r.reduce((e,t,a)=>e+t*o[a],0),c=[[t,i,n],[i,s,u],[n,u,l]],d=a=>{let i=e.map(e=>e[a]),n=i.reduce((e,t)=>e+t,0),s=function(e,t){let a=e=>e[0][0]*(e[1][1]*e[2][2]-e[1][2]*e[2][1])-e[0][1]*(e[1][0]*e[2][2]-e[1][2]*e[2][0])+e[0][2]*(e[1][0]*e[2][1]-e[1][1]*e[2][0]),r=a(e);if(1e-9>Math.abs(r))return null;let o=a=>e.map((e,r)=>e.map((e,o)=>o===a?t[r]:e));return[a(o(0))/r,a(o(1))/r,a(o(2))/r]}(c,[n,i.reduce((e,t,a)=>e+t*r[a],0),i.reduce((e,t,a)=>e+t*o[a],0)]);if(!s)return{a:n/t,b:0,d:0};let[l,u,d]=s;return{a:l,b:Math.sqrt(u*u+d*d),d:(Math.atan2(-d,u)/(2*Math.PI)+1)%1}},h=d(0),f=d(1),p=d(2);return{a:[h.a,f.a,p.a],b:[h.b,f.b,p.b],c:[.5,.5,.5],d:[h.d,f.d,p.d]}}var h=(e,t)=>{let a=s([r(i(e)),r(i(t))],17);return a[0]=i(e),a[a.length-1]=i(t),d(a)};function f(e){return 0===e.length?h(l.indigo,l.cyan):1===e.length?{a:i(e[0]),b:[0,0,0],c:[1,1,1],d:[0,0,0]}:d(s(e.map(e=>r(i(e))),17))}var p={prism:f(["#00C0E8","#6155F5","#CB30E0"]),berry:f(["#FF2D55","#CB30E0"]),lagoon:f(["#0088FF","#00C0E8","#34C759"]),citrus:f(["#34C759","#FFCC00","#FF8D28"]),azure:f(["#00C0E8","#0088FF","#6155F5"]),ember:f(["#FFCC00","#FF8D28","#FF2D55"])};function m(e){return e?"string"==typeof e?p[e]:e:p.prism}var g=`
attribute vec2 a;
void main(){ gl_Position = vec4(a, 0.0, 1.0); }
`,v=`
precision mediump float;
uniform vec2 uRes;
uniform float uTime;
uniform float uProgress;
uniform float uAlpha;
uniform float uBandTight;
uniform float uPosStart;
uniform float uPosEnd;
uniform float uHueShift;
uniform float uDirection; // 0 = horizontal, 1 = vertical
uniform float uWaveAmount;   // 0..2, multiplies edge wave displacement
uniform float uRippleAmount; // 0..2, multiplies vertical ripple intensity
uniform float uWaveSpeed;    // 0..3, multiplies all time-based motion
uniform float uBrightness;   // 0..1.5, scales the band's RGB before composite
uniform float uSwellAmount;  // 0..1, depth/iridescence intensity. 0 = flat band.
uniform vec3 uPalA;
uniform vec3 uPalB;
uniform vec3 uPalC;
uniform vec3 uPalD;

#define PI 3.14159265359

vec3 pal(in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d) {
  return a + b * cos(2.0 * PI * (c * t + d));
}

void main(){
  vec2 uv = gl_FragCoord.xy / uRes;
  float axis  = mix(uv.x, uv.y, uDirection);
  float cross = mix(uv.y, uv.x, uDirection);

  float pos = uPosStart + uProgress * (uPosEnd - uPosStart);

  float tw = uTime * uWaveSpeed;

  // One shallow bend keeps the silhouette organic without reading as a
  // wave. It moves less than 0.5% of the viewport and drifts slowly enough
  // to feel shaped rather than animated; richer motion stays inside the
  // foil texture below instead of distorting the band's sides.
  float waveX = sin(cross * 4.2 + tw * 0.08 + 0.3) * 0.004;
  waveX *= uWaveAmount;

  float d = (axis - pos) - waveX;
  float band = exp(-d * d * uBandTight);

  // Analytic slope of the band's pseudo-elevation map along the travel
  // axis only. We deliberately ignore the cross-axis chain-rule term
  // (\u2202waveX/\u2202cross) \u2014 letting the high-frequency edge wobble leak into
  // the normal made iridescence shimmer at the wave's frequency, which
  // read as "too wavy". Keeping the cross slope at zero gives a clean
  // left\u2192right hue sweep that matches the iOS name-drop feel.
  float dhDaxis = -2.0 * d * uBandTight * band;
  vec2 slope;
  slope.x = mix(dhDaxis, 0.0, uDirection);
  slope.y = mix(0.0, dhDaxis, uDirection);

  // Synthesised surface normal. The 0.18 gain controls perceived
  // height \u2014 higher = steeper flanks, more dramatic iridescent shift.
  vec3 N = normalize(vec3(-slope.x * 0.18, slope.y * 0.18, 1.0));

  float trail = clamp(0.5 - d * 1.3, 0.0, 1.0);
  trail = pow(trail, 2.5) * 0.30;

  // A dense crest and a very soft midpoint halo pull attention toward the
  // band while the page swaps. This improves perceptual masking without
  // turning the effect into a fullscreen cover or changing its choreography.
  float midpointFocus = 4.0 * uProgress * (1.0 - uProgress);
  float halo = exp(-d * d * 2.5) * 0.12 * midpointFocus;
  float intensity = max(band, trail);
  intensity = clamp(intensity + halo * (1.0 - band), 0.0, 1.0);

  // Cover the full screen edge-to-edge so fixed UI (tabs, toggles) is hidden
  // during the sweep. A barely-perceptible 1.5% fade keeps the look soft
  // without exposing pixels at the viewport edges.
  float vfade = smoothstep(0.0, 0.015, cross)
              * (1.0 - smoothstep(0.985, 1.0, cross));

  // Hue rotates with the synthesised normal \u2014 the trick that reads as
  // iOS-name-drop iridescence \u2014 but on a deliberately gentle scale so
  // the foil shift looks calm, not strobing.
  // Ripple changes the foil texture, not the edge geometry. Keeping those
  // responsibilities separate lets the band stay calm while its colour
  // still feels alive.
  float ripple = sin(cross * 12.0 + axis * 3.0 + tw * 0.40)
               * 0.015 * uRippleAmount;
  float t = N.x * 0.12 + N.y * 0.08
          + axis * 0.90 + cross * 0.16
          + ripple + uHueShift + uTime * 0.04;
  // Low-pass the cosine palette across neighbouring samples. This keeps
  // adjacent hues flowing into one another instead of forming hard colour
  // lanes when the surface normal changes around the crest.
  vec3 col = pal(t, uPalA, uPalB, uPalC, uPalD) * 0.50
           + pal(t - 0.18, uPalA, uPalB, uPalC, uPalD) * 0.25
           + pal(t + 0.18, uPalA, uPalB, uPalC, uPalD) * 0.25;
  col *= uBrightness;

  // Fixed key light + camera looking down +z. View-independent because
  // there's no real camera; this gives a stable highlight that travels
  // across the crest as the band moves, instead of one that wobbles with
  // viewport size.
  vec3 V = vec3(0.0, 0.0, 1.0);
  vec3 L = normalize(vec3(0.35, 0.55, 0.9));
  vec3 H = normalize(L + V);
  float NdotH = clamp(dot(N, H), 0.0, 1.0);
  float NdotV = clamp(dot(N, V), 0.0, 1.0);
  float fresnel = pow(1.0 - NdotV, 3.0);
  float spec    = pow(NdotH, 80.0);

  // Edge fade: as the band's traversal progress nears 0 or 1 (entering or
  // exiting the screen) the band reads at 20% alpha; at midpoint it's at
  // 100%. Softens the band's appearance/disappearance so it doesn't pop
  // into existence at full strength.
  float entryFade = mix(0.2, 1.0, 4.0 * uProgress * (1.0 - uProgress));

  // Body \u2014 palette colour where the band has presence. Premultiplied.
  float bodyA = intensity * vfade * uAlpha * entryFade;
  vec3  bodyPM = col * bodyA;

  // Highlights are emissive \u2014 they add light without occluding the page,
  // gated to the band's body so they only fire on the crest, not the wake.
  float highMask = band * vfade * uAlpha * entryFade * uSwellAmount;
  vec3  highEmit = (col * fresnel * 0.55 + vec3(spec) * 1.1) * highMask;
  float highA    = (fresnel * 0.4 + spec * 0.9) * highMask;

  gl_FragColor = vec4(bodyPM + highEmit, min(bodyA + highA, 1.0));
}
`,x={asymmetricSwell:0,secondaryCrest:0,refraction:0,chromaticDispersion:0,noiseEdge:0,cameraSweep:0,bloom:0,sparkles:0,curlWake:0},b=`
attribute vec3 aPos;
attribute vec2 aUV;
uniform float uProgress;
uniform float uPosStart;
uniform float uPosEnd;
uniform float uBandTight;
uniform float uDirection;
uniform float uTime;
uniform float uWaveAmount;
uniform float uWaveSpeed;
uniform float uElevation;
uniform float uDistance;
uniform float uAspect;
uniform mat4  uProj;
// Idea blending amounts. 0 = base behaviour, 1 = idea fully applied.
uniform float uIdea1Asym;
uniform float uIdea2Secondary;
uniform float uIdea5Noise;
uniform float uIdea6Camera;
uniform float uIdea9Curl;
varying vec2  vUV;
varying float vZ;
varying float vD;
varying float vBand;

// 1-input hash (Inigo Quilez / Dave_Hoskins style). Cheap, no texture.
float hash21(vec2 p) {
  p = fract(p * vec2(123.34, 456.21));
  p += dot(p, p + 45.32);
  return fract(p.x * p.y);
}

void main() {
  float axis  = mix(aUV.x, aUV.y, uDirection);
  float cross = mix(aUV.y, aUV.x, uDirection);
  float pos   = uPosStart + uProgress * (uPosEnd - uPosStart);
  float tw    = uTime * uWaveSpeed;

  // Base sine-harmonic edge wave. Same recipe as the fullscreen shader.
  float waveSine =
      sin(cross *  6.0 + tw * 1.3) * 0.020
    + sin(cross * 13.0 - tw * 0.9 + 1.4) * 0.012
    + sin(cross * 21.0 + tw * 1.7 + 2.6) * 0.006;

  // Idea 5: replace the sine harmonics with hash noise so the crest
  // line stops feeling periodic. Sampled in (cross, time) so it
  // animates rather than freezing in place. Blended via uIdea5Noise.
  float waveNoise = (hash21(vec2(cross * 7.5, tw * 0.45)) - 0.5) * 0.05;
  float waveX = mix(waveSine, waveNoise, uIdea5Noise) * uWaveAmount;

  float d = (axis - pos) - waveX;

  // Idea 1: asymmetric swell \u2014 leading face steeper, trailing wake
  // gentler. step(0, d) picks the leading side (d > 0 means we're
  // ahead of the crest), so we apply a higher tightness there and a
  // lower one in the wake.
  float kLead  = uBandTight * mix(1.0, 1.7, uIdea1Asym);
  float kTrail = uBandTight * mix(1.0, 0.55, uIdea1Asym);
  float k = mix(kTrail, kLead, step(0.0, d));
  float band = exp(-d * d * k);

  // Idea 2: secondary crest \u2014 a smaller bump trailing the primary peak
  // at d \u2248 0.12. Adds to band so both the elevation and the FS-side
  // intensity get the doubled silhouette.
  float secondary = exp(-(d - 0.12) * (d - 0.12) * uBandTight * 2.5) * 0.4;
  band += secondary * uIdea2Secondary;
  band = clamp(band, 0.0, 1.4);

  vec3 p = aPos;
  // Stretch the plane horizontally by uAspect so it always fills the
  // viewport regardless of canvas aspect; vertical stays at [-1, 1].
  p.x *= uAspect;
  p.z = band * uElevation;

  // Idea 9: curl wake \u2014 vertices behind the crest are pulled back
  // along the travel axis, mimicking the sticker peel's "the material
  // folds over itself instead of hovering" trick. exp(d*4) decays the
  // pull as we move further into the wake. Only applies for d < 0.
  float wakeMask = exp(min(d, 0.0) * 4.0) * (1.0 - step(0.0, d));
  vec2 axisDirUV = vec2(mix(1.0, 0.0, uDirection), mix(0.0, 1.0, uDirection));
  p.xy -= axisDirUV * 0.35 * wakeMask * uIdea9Curl;

  // Idea 6: camera sweep \u2014 gentle dolly that pushes the crest forward
  // when the band is in the middle of its traversal. sin(progress*PI)
  // peaks at uProgress=0.5 and is zero at the ends, so the dolly is
  // synced to the band, not the clock.
  float dolly = sin(uProgress * 3.14159) * 0.55 * uIdea6Camera;

  vec3 viewP = vec3(p.x, p.y, -uDistance + p.z + dolly);
  vUV = aUV;
  vZ = viewP.z;
  vD = d;
  vBand = band;
  gl_Position = uProj * vec4(viewP, 1.0);
}
`,w=`
#extension GL_OES_standard_derivatives : enable
precision highp float;
varying vec2  vUV;
varying float vZ;
varying float vD;
varying float vBand;
uniform vec2  uRes;
uniform float uTime;
uniform float uProgress;
uniform float uAlpha;
uniform float uDirection;
uniform float uHueShift;
uniform float uBrightness;
uniform float uSwellAmount;
uniform float uAspect;
uniform float uDistance;
uniform vec3  uPalA;
uniform vec3  uPalB;
uniform vec3  uPalC;
uniform vec3  uPalD;
// FS-side idea amounts (matches uIdeaN naming used in VS for clarity).
uniform float uIdea3Refraction;
uniform float uIdea4Chromatic;
uniform float uIdea7Bloom;
uniform float uIdea8Sparkles;
// HTML-in-canvas texture support \u2014 when uUseTexture > 0, the band's
// body samples from this texture (the live page rasterised via
// drawElementImage) instead of emitting pure palette colour. The
// page swells with the mesh's z-displacement plus an N.xy-driven UV
// offset (uRefractStrength) that lenses light at the rim.
uniform sampler2D uTex;
uniform float uUseTexture;
uniform float uRefractStrength;
// MARGIN value the JS-side projection uses \u2014 needed to remap plane UV
// (which spans the overdrawn plane) into viewport UV (the un-displaced
// screen position) for the texture sample. Otherwise the page texture
// reads zoomed-in by 1/MARGIN.
uniform float uTexMargin;

#define PI 3.14159265359

vec3 pal(float t, vec3 a, vec3 b, vec3 c, vec3 d) {
  return a + b * cos(2.0 * PI * (c * t + d));
}

float hash21(vec2 p) {
  p = fract(p * vec2(123.34, 456.21));
  p += dot(p, p + 45.32);
  return fract(p.x * p.y);
}

void main() {
  // Real screen-space normal from vZ derivatives. The 90.0 gain is the
  // same one sticker-gl.ts uses; it balances tilt sensitivity vs.
  // per-pixel noise from finite-difference normals.
  vec3 N = normalize(vec3(-dFdx(vZ) * 90.0, dFdy(vZ) * 90.0, 1.0));

  // View-space surface point (matches sticker-gl.ts's camera setup).
  vec3 surfPos = vec3((vUV.x - 0.5) * 2.0 * uAspect,
                      (0.5 - vUV.y) * 2.0,
                      vZ);
  // Fixed key light, parked off-screen up-and-to-the-left.
  vec3 lightPos = vec3(-0.4, 0.6, -uDistance + 1.6);
  vec3 L = normalize(lightPos - surfPos);
  vec3 V = normalize(-surfPos);
  vec3 H = normalize(L + V);
  vec3 R = reflect(-L, N);

  float NdotH = clamp(dot(N, H), 0.0, 1.0);
  float NdotV = clamp(dot(N, V), 0.0, 1.0);

  // Trailing wake \u2014 same dark-side glow that the flat shader uses, so
  // the band still has something behind it instead of a hard cutoff.
  float trail = clamp(0.5 - vD * 1.3, 0.0, 1.0);
  trail = pow(trail, 2.5) * 0.30;
  float intensity = max(vBand * 0.95, trail);

  float axis = mix(vUV.x, vUV.y, uDirection);
  // Edge taper driven by *screen-space* coords, not plane UV. The mesh
  // overdraws the viewport (MARGIN < 1 on the JS side), so plane-UV
  // edges sit off-screen and a UV-based fade would be invisible.
  // Sampling gl_FragCoord keeps the fade tied to actual viewport edges.
  vec2 screenUV = gl_FragCoord.xy / uRes;
  float screenCross = mix(screenUV.y, screenUV.x, uDirection);
  float vfade = smoothstep(0.0, 0.015, screenCross) * smoothstep(1.0, 0.985, screenCross);

  // Iridescence \u2014 hue driven primarily by the reflection vector (which
  // rotates as the crest rises and falls), with a slow secondary drift
  // from axis + time. This is the bit that reads as iOS-name-drop foil.
  float hueT = R.x * 1.1 + R.y * 0.7
             + axis * 0.6
             + uHueShift + uTime * 0.04;

  // Idea 4: chromatic dispersion \u2014 sample the palette at three offset
  // hues and recombine per RGB channel. Reads as a prism split on
  // edges where the normal tilts hard.
  float disp = 0.08 * uIdea4Chromatic;
  vec3 colCenter = pal(hueT,        uPalA, uPalB, uPalC, uPalD);
  vec3 colR      = pal(hueT + disp, uPalA, uPalB, uPalC, uPalD);
  vec3 colB      = pal(hueT - disp, uPalA, uPalB, uPalC, uPalD);
  vec3 colSplit  = vec3(colR.r, colCenter.g, colB.b);
  vec3 col = mix(colCenter, colSplit, uIdea4Chromatic) * uBrightness;

  float fresnel = pow(1.0 - NdotV, 3.0);
  float spec    = pow(NdotH, 64.0);

  // Idea 7: bloom (fake) \u2014 adds a wider, softer halo around the tight
  // catch-light. Real bloom would need an FBO + blur; this is the
  // cheapest 1-pass approximation that still reads as glow.
  float bloomHalo = pow(NdotH, 8.0) * uIdea7Bloom * 0.85;

  float entryFade = mix(0.2, 1.0, 4.0 * uProgress * (1.0 - uProgress));

  // Idea 3: fake refraction \u2014 when the surface tilts hard (low N.z),
  // body alpha drops so the page behind shows through. Reads as a
  // lens bending light rather than a coloured stripe. Body alpha is
  // multiplied by N.z (mapped through uIdea3Refraction).
  float refractMask = mix(1.0, N.z, uIdea3Refraction * 0.85);

  float bodyA  = intensity * vfade * uAlpha * entryFade * refractMask;
  vec3  bodyPM = col * bodyA;

  // Texture mode: read the live page (rasterised via html-in-canvas)
  // at a UV that's been offset by the screen-space normal. The
  // mesh's z displacement already foreshortens the texture over the
  // crest; this extra refraction adds glass-lens lateral bend, which
  // is what reads as "the page is actually swelling" rather than just
  // being colour-tinted. Texture sample is opaque so we override the
  // body's premultiplied colour + alpha completely.
  if (uUseTexture > 0.5) {
    // Remap plane UV \u2192 viewport UV. The plane overdraws the viewport
    // by 1/uTexMargin so the visible region only spans the central
    // strip of vUV; rescale it to span [0, 1] of the texture.
    vec2 viewportUV = (vUV - 0.5) / uTexMargin + 0.5;
    // FLIP_Y=true is set at upload (matches THREE.CanvasTexture's
    // default). With that flip, GL texture-Y=0 corresponds to the
    // page TOP. Our mesh has vUV.y=0 at the viewport top, so we
    // sample at viewportUV directly with no further inversion.
    vec2 sampleUV = viewportUV + N.xy * uRefractStrength * vBand;
    vec3 page = texture2D(uTex, clamp(sampleUV, vec2(0.0), vec2(1.0))).rgb;
    // Body composite: page texture as base everywhere; iridescent
    // band colour ADDED on top, weighted by vBand. Mixing was
    // washing out the colour against the typical white page
    // background. Additive composition keeps the page legible
    // away from the band and lets the band read as a bright
    // overlay where it passes \u2014 like a glossy iridescent strip
    // sliding across the page.
    bodyA = vfade * uAlpha;
    vec3 bandOverlay = col * vBand * 1.6;
    bodyPM = (page + bandOverlay) * bodyA;
  }

  float highMask = vBand * vfade * uAlpha * entryFade * uSwellAmount;
  vec3  highEmit = (col * fresnel * 0.55 + vec3(spec) * 1.2 + col * bloomHalo) * highMask;
  float highA    = (fresnel * 0.4 + spec * 0.9 + bloomHalo * 0.7) * highMask;

  // Idea 8: wake sparkles \u2014 tile-quantised hash noise sampled at a
  // coarse grid so the twinkle is point-like, not a uniform mist.
  // Restricted to the wake region (where trail > 0) so the body of
  // the band stays clean.
  float sGrid = 140.0;
  float sSeed = hash21(floor(vUV * sGrid + floor(uTime * 14.0)));
  float sparkle = step(0.965, sSeed) * trail * uIdea8Sparkles;
  highEmit += vec3(sparkle * 1.6) * vfade * uAlpha;
  highA    += sparkle * 1.0 * vfade * uAlpha;

  gl_FragColor = vec4(bodyPM + highEmit, clamp(bodyA + highA, 0.0, 1.0));
}
`,A=`
attribute vec3 aPos;
attribute vec2 aUV;
uniform float uProgress;
uniform vec2  uAnchor;
uniform float uBulgeRadius;
uniform float uElevation;
uniform float uAspect;
uniform float uDistance;
uniform float uTravelMode;
uniform mat4  uProj;
varying vec2  vUV;
varying float vZ;
varying float vBulge;

#define PI 3.14159265359

void main() {
  // Travel mode: anchor.x linearly traverses [-0.2, 1.2] as progress
  // goes 0\u21921 (matches the project's sweep posStart/posEnd convention).
  // Static mode: anchor stays at uAnchor.
  vec2 anchorPos = mix(uAnchor,
                       vec2(mix(-0.2, 1.2, uProgress), uAnchor.y),
                       uTravelMode);

  // Aspect-correct radial distance from anchor in UV space. Without
  // the aspect correction the bulge would render as a vertical ellipse
  // on wide viewports; this keeps it circular.
  vec2 d = (aUV - anchorPos) * vec2(uAspect, 1.0);
  float r = length(d);

  // Envelope:
  //   Static mode: sin(p\xb7\u03C0) grows to peak at progress=0.5 then
  //     deflates by progress=1.0 \u2014 the bulge swells in place.
  //   Travel mode: envelope stays at 1.0 \u2014 the bulge has constant
  //     amplitude as it traverses; it naturally fades in/out at the
  //     viewport edges as the anchor enters/leaves the screen.
  float envelope = mix(sin(uProgress * PI), 1.0, uTravelMode);

  // Radial profile \u2014 gaussian falloff inside the bulge. Effective
  // radius scales with envelope so the bulge visibly expands outward
  // as it grows, not just rises in place.
  float effR = max(uBulgeRadius * envelope, 0.001);
  float profile = exp(-(r * r) / (effR * effR));
  // Final bulge magnitude \u2014 combines envelope (overall height) with
  // radial profile (height at this vertex).
  float bulge = profile * envelope;

  vec3 p = aPos;
  // Stretch plane horizontally by aspect so it always fills the
  // viewport regardless of canvas dimensions.
  p.x *= uAspect;
  // Lift the vertex along +z toward the camera. The bulge centre
  // moves closest, edges stay flat. With perspective projection this
  // makes the centre's pixels expand outward, which reads as the
  // page surface "rising toward the screen" \u2014 the NameDrop feel.
  p.z = bulge * uElevation;

  vec3 viewP = vec3(p.x, p.y, -uDistance + p.z);
  vUV = aUV;
  vZ = viewP.z;
  vBulge = bulge;
  gl_Position = uProj * vec4(viewP, 1.0);
}
`,y=`
#extension GL_OES_standard_derivatives : enable
precision highp float;
varying vec2  vUV;
varying float vZ;
varying float vBulge;
uniform vec2  uRes;
uniform float uTime;
uniform float uProgress;
uniform float uAlpha;
uniform float uHueShift;
uniform float uAspect;
uniform float uDistance;
uniform float uIridescence;
uniform float uRefractStrength;
uniform float uTexMargin;
uniform sampler2D uTex;
uniform float uUseTexture;
// Wipe support \u2014 when uHasSnapshot is on, uTexOld holds a frozen
// snapshot of the page as it was at sweep-start. The FS mixes
// between uTex (live, "after") and uTexOld ("before") based on
// whether the fragment's viewport X is left or right of the bulge
// anchor \u2014 producing a wipe that reveals the new page as the sweep
// crosses, instead of just showing the live page everywhere with a
// bulge on top.
uniform sampler2D uTexOld;
uniform float uHasSnapshot;
uniform vec3  uPalA;
uniform vec3  uPalB;
uniform vec3  uPalC;
uniform vec3  uPalD;

#define PI 3.14159265359

vec3 pal(float t, vec3 a, vec3 b, vec3 c, vec3 d) {
  return a + b * cos(2.0 * PI * (c * t + d));
}

void main() {
  // Real screen-space normal from z derivatives \u2014 same trick as the
  // mesh sweep and the sticker peel. The 90.0 gain matches sticker-
  // gl.ts; balances tilt sensitivity vs. per-pixel noise on the
  // bulge's flanks.
  vec3 N = normalize(vec3(-dFdx(vZ) * 90.0, dFdy(vZ) * 90.0, 1.0));

  // Plane-UV \u2192 viewport-UV remap (mesh overdraws viewport by
  // 1/uTexMargin so plane UV [0,1] covers more than just the visible
  // area; rescale to map the visible region to texture [0,1]).
  vec2 viewportUV = (vUV - 0.5) / uTexMargin + 0.5;

  // Refraction offset: sample the page at a UV shifted by the
  // surface normal. On flat plate, N.xy \u2248 0 so no shift. On bulge
  // flanks where N tilts hardest, the page lenses outward, which
  // reads as glass-like refraction at the bulge rim.
  vec2 sampleUV = viewportUV + N.xy * uRefractStrength * vBulge;
  // pageA tracks whether we have a real page texture to render.
  // When uUseTexture is 0 (host hasn't enabled it yet, or browser
  // lacks drawElementImage) we render the bulge with TRANSPARENT
  // background instead of opaque white \u2014 the source canvas's normal
  // DOM rendering shows through the GL canvas everywhere except the
  // raised bulge area. Without this guard the GL canvas paints
  // opaque white during the first 1-2 frames of every sweep (before
  // the host's pump catches up) and the page appears to flash white.
  vec3 page = vec3(0.0);
  float pageA = 0.0;
  if (uUseTexture > 0.5) {
    vec2 clampedUV = clamp(sampleUV, vec2(0.0), vec2(1.0));
    vec3 pageLive = texture2D(uTex, clampedUV).rgb;
    if (uHasSnapshot > 0.5) {
      // Wipe mode. Project the bulge's travel-mode anchor X (plane
      // UV) into viewport UV space (matching the remap above) so we
      // can compare it to viewportUV.x. The bulge centre is the
      // wipe boundary: fragments left of it have already been
      // "passed over" (show the live/new page), fragments right of
      // it haven't yet (show the snapshot/old page). A smoothstep
      // softens the boundary into a band the width of the bulge's
      // visible falloff.
      float anchorPlaneX = mix(-0.2, 1.2, uProgress);
      float anchorViewX = (anchorPlaneX - 0.5) / uTexMargin + 0.5;
      float wipeT = smoothstep(anchorViewX - 0.08, anchorViewX + 0.08, viewportUV.x);
      vec3 pageOld = texture2D(uTexOld, clampedUV).rgb;
      // wipeT = 0 left of bulge \u2192 live (new page)
      // wipeT = 1 right of bulge \u2192 snapshot (old page)
      page = mix(pageLive, pageOld, wipeT);
    } else {
      page = pageLive;
    }
    pageA = 1.0;
  }

  // View/light setup. Light parked off-screen up-and-to-the-left so
  // the bulge has a consistent specular catch on its left flank as
  // it expands.
  vec3 surfPos = vec3((vUV.x - 0.5) * 2.0 * uAspect,
                      (0.5 - vUV.y) * 2.0,
                      vZ);
  vec3 lightPos = vec3(-0.5, 0.7, -uDistance + 1.6);
  vec3 L = normalize(lightPos - surfPos);
  vec3 V = normalize(-surfPos);
  vec3 H = normalize(L + V);
  vec3 R = reflect(-L, N);

  float NdotH = clamp(dot(N, H), 0.0, 1.0);
  float NdotV = clamp(dot(N, V), 0.0, 1.0);

  // Screen-edge taper so the bulge can dissolve cleanly into the
  // viewport edges without exposing seams from the overdrawn plane.
  // Uses (1.0 - smoothstep(0.985, 1.0, x)) instead of the reversed-
  // arg smoothstep(1.0, 0.985, x): the latter is undefined behaviour
  // per the GLSL ES spec (edge0 must be < edge1) and some drivers
  // return 0 which would zero out vfade and make the shader invisible.
  vec2 screenUV = gl_FragCoord.xy / uRes;
  float vfade = smoothstep(0.0, 0.015, screenUV.x) * (1.0 - smoothstep(0.985, 1.0, screenUV.x))
              * smoothstep(0.0, 0.015, screenUV.y) * (1.0 - smoothstep(0.985, 1.0, screenUV.y));

  // Iridescent palette \u2014 hue driven primarily by the reflection
  // vector (rotates with normal tilt) plus a slow time/spatial
  // drift. This is what reads as NameDrop's foil shimmer.
  float hueT = R.x * 1.4 + R.y * 0.9
             + viewportUV.x * 0.3 + viewportUV.y * 0.2
             + uHueShift + uTime * 0.06;
  vec3 iri = pal(hueT, uPalA, uPalB, uPalC, uPalD);

  // Bulge surface highlights \u2014 Fresnel rim catches the bulge's
  // silhouette; tight specular gives a glass catch-light on the
  // crest. Both gated by vBulge so the flat plate stays clean.
  float fresnel = pow(1.0 - NdotV, 3.0) * vBulge;
  float spec    = pow(NdotH, 96.0) * vBulge;

  // Chromatic dispersion at the bulge edges \u2014 sample the palette
  // at three offset hues per RGB channel where the normal tilts
  // hard. Gives the rim a prismatic split, the NameDrop touch that
  // separates it from a generic radial gradient.
  float dispersion = (1.0 - NdotV) * vBulge * 0.06;
  vec3 iriR = pal(hueT + dispersion, uPalA, uPalB, uPalC, uPalD);
  vec3 iriB = pal(hueT - dispersion, uPalA, uPalB, uPalC, uPalD);
  vec3 iriSplit = vec3(iriR.r, iri.g, iriB.b);

  // Composite. Two modes:
  //   uUseTexture=1 (pageA=1): page covers viewport opaquely; bulge
  //     warps it visually + iridescent shimmer overlays the bulge.
  //   uUseTexture=0 (pageA=0): GL stays transparent except for the
  //     bulge area \u2014 the source canvas's normal DOM rendering shows
  //     through everywhere else.
  //
  // bodyA controls the GL canvas's opacity at this fragment. In
  // texture mode it's fully opaque (we *are* the page). Outside
  // texture mode it follows vBulge so flat areas read transparent.
  float bulgePresence = clamp(vBulge * 1.4, 0.0, 1.0);
  float bodyA = pageA + (1.0 - pageA) * bulgePresence;
  // bodyRgb is the page texture in texture mode; in fallback mode
  // it's just the iridescent palette colour weighted by vBulge so
  // the bulge area has *something* to show even with no texture.
  vec3 bodyRgb = page + (1.0 - pageA) * iri * vBulge;

  vec3 iriOverlay = iriSplit * vBulge * uIridescence * 0.55;
  vec3 highlights = iri * fresnel * 0.6 + vec3(spec) * 1.3;
  vec3 col = bodyRgb + iriOverlay + highlights * uIridescence;

  // Same reversed-arg avoidance as vfade above \u2014 entryFade ramps in
  // over progress 0..0.05, then out over 0.95..1.0.
  float entryFade = smoothstep(0.0, 0.05, uProgress) * (1.0 - smoothstep(0.95, 1.0, uProgress));
  float alpha = bodyA * vfade * uAlpha * entryFade;

  // Premultiplied alpha output (matches the project's blendFunc:
  // ONE, ONE_MINUS_SRC_ALPHA).
  gl_FragColor = vec4(col * alpha, alpha);
}
`;function T(e,t,a,r){let o=t=>3*(1-t)*(1-t)*t*e+3*(1-t)*t*t*a+t*t*t,i=t=>3*(1-4*t+3*t*t)*e+3*(2*t-3*t*t)*a+3*t*t;return e=>{let a;if(e<=0)return 0;if(e>=1)return 1;let n=e;for(let t=0;t<8;t++){let t=o(n)-e;if(1e-6>Math.abs(t))break;let a=i(n);if(1e-6>Math.abs(a))break;n-=t/a}return 3*(1-(a=n))*(1-a)*a*t+3*(1-a)*a*a*r+a*a*a}}var E={linear:e=>e,easeOutQuart:e=>1-Math.pow(1-e,4),easeOutCubic:e=>1-Math.pow(1-e,3),easeInCubic:e=>e*e*e,easeInOutCubic:e=>e<.5?4*e*e*e:1-Math.pow(-2*e+2,3)/2,easeOutExpo:e=>1===e?1:1-Math.pow(2,-10*e),easeInOutQuint:e=>e<.5?16*e*e*e*e*e:1-Math.pow(-2*e+2,5)/2,snap:T(1,0,.35,.95),ease:T(.25,.1,.25,1),back:T(.175,.885,.32,1.1)},S=e=>e?"function"==typeof e?e:E[e]:E.easeOutQuart,P=e=>1-Math.pow(1-e,4),R=e=>e<.5?4*e*e*e:1-Math.pow(-2*e+2,3)/2;e.s(["ACCENTS",0,l,"ACCENT_ORDER",0,u,"EASINGS",0,E,"PALETTES",0,p,"accentChain",0,f,"accentPair",0,h,"createMeshShader",0,function(e={}){if("u"<typeof window)return null;let t=e.canvas??document.createElement("canvas"),a=t.getContext("webgl",{alpha:!0,antialias:!0,premultipliedAlpha:!0});if(!a)return null;if(!a.getExtension("OES_standard_derivatives"))return console.warn("[glimm] OES_standard_derivatives unavailable — mesh sweep will lack proper lighting; falling back is the caller's responsibility"),null;let r=(e,t)=>{let r=a.createShader(e);return a.shaderSource(r,t),a.compileShader(r),a.getShaderParameter(r,a.COMPILE_STATUS)||console.error("[glimm] mesh shader compile error:",a.getShaderInfoLog(r)),r},o=r(a.VERTEX_SHADER,b),i=r(a.FRAGMENT_SHADER,w),n=a.createProgram();a.attachShader(n,o),a.attachShader(n,i),a.linkProgram(n),a.useProgram(n);let s=e.cols??96,l=e.rows??56,u=1,c=[],d=[],h=[];for(let e=0;e<=l;e++){let t=e/l,a=(1-t)*2-1;for(let e=0;e<=s;e++){let r=e/s,o=2*r-1;c.push(o,a,0),d.push(r,t)}}for(let e=0;e<l;e++)for(let t=0;t<s;t++){let a=e*(s+1)+t,r=a+1,o=a+(s+1),i=o+1;h.push(a,r,o,r,i,o)}let f=a.createBuffer();a.bindBuffer(a.ARRAY_BUFFER,f),a.bufferData(a.ARRAY_BUFFER,new Float32Array(c),a.STATIC_DRAW);let p=a.getAttribLocation(n,"aPos");a.enableVertexAttribArray(p),a.vertexAttribPointer(p,3,a.FLOAT,!1,0,0);let g=a.createBuffer();a.bindBuffer(a.ARRAY_BUFFER,g),a.bufferData(a.ARRAY_BUFFER,new Float32Array(d),a.STATIC_DRAW);let v=a.getAttribLocation(n,"aUV");a.enableVertexAttribArray(v),a.vertexAttribPointer(v,2,a.FLOAT,!1,0,0);let A=a.createBuffer();a.bindBuffer(a.ELEMENT_ARRAY_BUFFER,A),a.bufferData(a.ELEMENT_ARRAY_BUFFER,new Uint16Array(h),a.STATIC_DRAW);let y=a.getUniformLocation(n,"uRes"),T=a.getUniformLocation(n,"uProgress"),E=a.getUniformLocation(n,"uPosStart"),S=a.getUniformLocation(n,"uPosEnd"),P=a.getUniformLocation(n,"uBandTight"),R=a.getUniformLocation(n,"uDirection"),U=a.getUniformLocation(n,"uTime"),k=a.getUniformLocation(n,"uAlpha"),L=a.getUniformLocation(n,"uHueShift"),M=a.getUniformLocation(n,"uWaveAmount"),_=a.getUniformLocation(n,"uWaveSpeed"),F=a.getUniformLocation(n,"uElevation"),B=a.getUniformLocation(n,"uBrightness"),C=a.getUniformLocation(n,"uSwellAmount"),D=a.getUniformLocation(n,"uAspect"),N=a.getUniformLocation(n,"uDistance"),I=a.getUniformLocation(n,"uProj"),V=a.getUniformLocation(n,"uPalA"),j=a.getUniformLocation(n,"uPalB"),z=a.getUniformLocation(n,"uPalC"),O=a.getUniformLocation(n,"uPalD"),H=a.getUniformLocation(n,"uIdea1Asym"),W=a.getUniformLocation(n,"uIdea2Secondary"),G=a.getUniformLocation(n,"uIdea3Refraction"),X=a.getUniformLocation(n,"uIdea4Chromatic"),Z=a.getUniformLocation(n,"uIdea5Noise"),$=a.getUniformLocation(n,"uIdea6Camera"),Y=a.getUniformLocation(n,"uIdea7Bloom"),q=a.getUniformLocation(n,"uIdea8Sparkles"),K=a.getUniformLocation(n,"uIdea9Curl"),Q=a.getUniformLocation(n,"uTex"),J=a.getUniformLocation(n,"uUseTexture"),ee=a.getUniformLocation(n,"uRefractStrength"),et=a.getUniformLocation(n,"uTexMargin"),ea=null;a.uniform1f(N,3.4),a.enable(a.BLEND),a.blendFunc(a.ONE,a.ONE_MINUS_SRC_ALPHA);let er={progress:0,alpha:0,palette:e.palette??m(void 0),bandTight:e.bandTight??14,direction:e.direction??"ltr",waveAmount:e.waveAmount??1,rippleAmount:e.rippleAmount??1,waveSpeed:e.waveSpeed??1,brightness:e.brightness??1,swellAmount:e.swellAmount??.9,elevation:e.elevation??.18,ideas:{...x,...e.ideas??{}},textureSource:null,useTexture:0,refractStrength:.025},eo=.4*Math.random(),ei=()=>{var e,r;let o,i=Math.min(window.devicePixelRatio||1,2),s=t.getBoundingClientRect(),l=Math.max(1,Math.round(s.width*i)),c=Math.max(1,Math.round(s.height*i));(t.width!==l||t.height!==c)&&(t.width=l,t.height=c,a.viewport(0,0,l,c)),u=Math.max(.01,l/Math.max(1,c));let d=(e=2*Math.atan(.20588235294117646),r=u,new Float32Array([(o=1/Math.tan(e/2))/r,0,0,0,0,o,0,0,0,0,-1.02020202020202,-1,0,0,-.20202020202020202,0]));a.useProgram(n),a.uniformMatrix4fv(I,!1,d),a.uniform1f(D,u)};ei();let en=new ResizeObserver(ei);en.observe(t);let es=performance.now(),el=0,eu=()=>{let e=(performance.now()-es)/1e3,r=(e=>{switch(e){case"ltr":return{axis:0,posStart:-.2,posEnd:1.2};case"rtl":return{axis:0,posStart:1.2,posEnd:-.2};case"ttb":return{axis:1,posStart:-.2,posEnd:1.2};case"btt":return{axis:1,posStart:1.2,posEnd:-.2}}})(er.direction);if(a.clearColor(0,0,0,0),a.clear(a.COLOR_BUFFER_BIT),a.uniform2f(y,t.width,t.height),a.uniform1f(U,e),a.uniform1f(T,er.progress),a.uniform1f(k,er.alpha),a.uniform1f(P,er.bandTight),a.uniform1f(E,r.posStart),a.uniform1f(S,r.posEnd),a.uniform1f(R,r.axis),a.uniform1f(M,er.waveAmount),a.uniform1f(_,er.waveSpeed),a.uniform1f(B,er.brightness),a.uniform1f(C,er.swellAmount),a.uniform1f(F,er.elevation),a.uniform1f(L,eo),a.uniform3f(V,er.palette.a[0],er.palette.a[1],er.palette.a[2]),a.uniform3f(j,er.palette.b[0],er.palette.b[1],er.palette.b[2]),a.uniform3f(z,er.palette.c[0],er.palette.c[1],er.palette.c[2]),a.uniform3f(O,er.palette.d[0],er.palette.d[1],er.palette.d[2]),a.uniform1f(H,er.ideas.asymmetricSwell),a.uniform1f(W,er.ideas.secondaryCrest),a.uniform1f(G,er.ideas.refraction),a.uniform1f(X,er.ideas.chromaticDispersion),a.uniform1f(Z,er.ideas.noiseEdge),a.uniform1f($,er.ideas.cameraSweep),a.uniform1f(Y,er.ideas.bloom),a.uniform1f(q,er.ideas.sparkles),a.uniform1f(K,er.ideas.curlWake),er.useTexture>0&&er.textureSource){ea||(ea=a.createTexture(),a.bindTexture(a.TEXTURE_2D,ea),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_WRAP_S,a.CLAMP_TO_EDGE),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_WRAP_T,a.CLAMP_TO_EDGE),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_MIN_FILTER,a.LINEAR),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_MAG_FILTER,a.LINEAR)),a.activeTexture(a.TEXTURE0),a.bindTexture(a.TEXTURE_2D,ea);try{a.pixelStorei(a.UNPACK_PREMULTIPLY_ALPHA_WEBGL,!1),a.pixelStorei(a.UNPACK_FLIP_Y_WEBGL,!1),a.texImage2D(a.TEXTURE_2D,0,a.RGBA,a.RGBA,a.UNSIGNED_BYTE,er.textureSource)}catch{}a.uniform1i(Q,0),a.uniform1f(J,1)}else a.uniform1f(J,0);a.uniform1f(ee,er.refractStrength),a.uniform1f(et,.7),a.drawElements(a.TRIANGLES,h.length,a.UNSIGNED_SHORT,0),el=requestAnimationFrame(eu)};return el=requestAnimationFrame(eu),{canvas:t,setProgress:e=>{er.progress=e},setAlpha:e=>{er.alpha=e},setPalette:e=>{er.palette=e},setBandTight:e=>{er.bandTight=e},setDirection:e=>{er.direction=e},setWaveAmount:e=>{er.waveAmount=e},setRippleAmount:e=>{er.rippleAmount=e},setWaveSpeed:e=>{er.waveSpeed=e},setBrightness:e=>{er.brightness=e},setSwellAmount:e=>{er.swellAmount=e},setElevation:e=>{er.elevation=e},setIdea:(e,t)=>{er.ideas={...er.ideas,[e]:t}},setIdeas:e=>{er.ideas={...e}},setTextureSource:e=>{er.textureSource=e},setUseTexture:e=>{er.useTexture=e},setRefractStrength:e=>{er.refractStrength=e},getProgress:()=>er.progress,getAlpha:()=>er.alpha,destroy:()=>{cancelAnimationFrame(el),en.disconnect(),a.deleteProgram(n),a.deleteBuffer(f),a.deleteBuffer(g),a.deleteBuffer(A),ea&&a.deleteTexture(ea),a.deleteShader(o),a.deleteShader(i)}}},"createNamedropShader",0,function(e={}){if("u"<typeof window)return null;let t=e.canvas??document.createElement("canvas"),a=t.getContext("webgl",{alpha:!0,antialias:!0,premultipliedAlpha:!0});if(!a)return null;if(!a.getExtension("OES_standard_derivatives"))return console.warn("[glimm/namedrop] OES_standard_derivatives unavailable — bulge lighting will be missing"),null;let r=(e,t)=>{let r=a.createShader(e);return a.shaderSource(r,t),a.compileShader(r),a.getShaderParameter(r,a.COMPILE_STATUS)||console.error("[glimm/namedrop] shader compile error:",a.getShaderInfoLog(r)),r},o=r(a.VERTEX_SHADER,A),i=r(a.FRAGMENT_SHADER,y),n=a.createProgram();if(a.attachShader(n,o),a.attachShader(n,i),a.linkProgram(n),a.useProgram(n),!a.getProgramParameter(n,a.LINK_STATUS))return console.error("[glimm/namedrop] program link error:",a.getProgramInfoLog(n)),null;console.log("[glimm/namedrop] shader created, canvas:",t.width,"x",t.height);let s=e.cols??96,l=e.rows??56,u=[],c=[],d=[];for(let e=0;e<=l;e++){let t=e/l,a=(1-t)*2-1;for(let e=0;e<=s;e++){let r=e/s,o=2*r-1;u.push(o,a,0),c.push(r,t)}}for(let e=0;e<l;e++)for(let t=0;t<s;t++){let a=e*(s+1)+t,r=a+1,o=a+(s+1),i=o+1;d.push(a,r,o,r,i,o)}let h=a.createBuffer();a.bindBuffer(a.ARRAY_BUFFER,h),a.bufferData(a.ARRAY_BUFFER,new Float32Array(u),a.STATIC_DRAW);let f=a.getAttribLocation(n,"aPos");a.enableVertexAttribArray(f),a.vertexAttribPointer(f,3,a.FLOAT,!1,0,0);let p=a.createBuffer();a.bindBuffer(a.ARRAY_BUFFER,p),a.bufferData(a.ARRAY_BUFFER,new Float32Array(c),a.STATIC_DRAW);let g=a.getAttribLocation(n,"aUV");a.enableVertexAttribArray(g),a.vertexAttribPointer(g,2,a.FLOAT,!1,0,0);let v=a.createBuffer();a.bindBuffer(a.ELEMENT_ARRAY_BUFFER,v),a.bufferData(a.ELEMENT_ARRAY_BUFFER,new Uint16Array(d),a.STATIC_DRAW);let x=a.getUniformLocation(n,"uRes"),b=a.getUniformLocation(n,"uTime"),w=a.getUniformLocation(n,"uProgress"),T=a.getUniformLocation(n,"uAlpha"),E=a.getUniformLocation(n,"uHueShift"),S=a.getUniformLocation(n,"uAspect"),P=a.getUniformLocation(n,"uDistance"),R=a.getUniformLocation(n,"uAnchor"),U=a.getUniformLocation(n,"uBulgeRadius"),k=a.getUniformLocation(n,"uElevation"),L=a.getUniformLocation(n,"uIridescence"),M=a.getUniformLocation(n,"uRefractStrength"),_=a.getUniformLocation(n,"uTravelMode"),F=a.getUniformLocation(n,"uTexMargin"),B=a.getUniformLocation(n,"uTex"),C=a.getUniformLocation(n,"uUseTexture"),D=a.getUniformLocation(n,"uTexOld"),N=a.getUniformLocation(n,"uHasSnapshot"),I=a.getUniformLocation(n,"uProj"),V=a.getUniformLocation(n,"uPalA"),j=a.getUniformLocation(n,"uPalB"),z=a.getUniformLocation(n,"uPalC"),O=a.getUniformLocation(n,"uPalD"),H=null,W=null;a.uniform1f(P,3.4),a.uniform1f(F,.7),a.enable(a.BLEND),a.blendFunc(a.ONE,a.ONE_MINUS_SRC_ALPHA);let G={progress:0,alpha:0,palette:e.palette??m("prism"),anchor:e.anchor??[.5,.5],bulgeRadius:e.bulgeRadius??.55,elevation:e.elevation??.32,iridescence:e.iridescence??1,refractStrength:e.refractStrength??.04,travelMode:e.travelMode??0,direction:e.direction??"ltr",bandTight:e.bandTight??14,waveAmount:1,rippleAmount:1,waveSpeed:1,brightness:1,swellAmount:1,textureSource:null,useTexture:0,snapshotSource:null},X=.4*Math.random(),Z=1,$=()=>{var e,r;let o,i=Math.min(window.devicePixelRatio||1,2),s=t.getBoundingClientRect(),l=Math.max(1,Math.round(s.width*i)),u=Math.max(1,Math.round(s.height*i));(t.width!==l||t.height!==u)&&(t.width=l,t.height=u,a.viewport(0,0,l,u)),Z=Math.max(.01,l/Math.max(1,u));let c=(e=2*Math.atan(.20588235294117646),r=Z,new Float32Array([(o=1/Math.tan(e/2))/r,0,0,0,0,o,0,0,0,0,-1.02020202020202,-1,0,0,-.20202020202020202,0]));a.useProgram(n),a.uniformMatrix4fv(I,!1,c),a.uniform1f(S,Z)};$();let Y=new ResizeObserver($);Y.observe(t);let q=performance.now(),K=0,Q=!1,J=()=>{let e=(performance.now()-q)/1e3;if(!Q&&G.alpha>.001&&(Q=!0,console.log("[glimm/namedrop] first active frame — alpha:",G.alpha.toFixed(3),"progress:",G.progress.toFixed(3),"useTexture:",G.useTexture,"textureSource:",G.textureSource?"set":"null")),a.clearColor(0,0,0,0),a.clear(a.COLOR_BUFFER_BIT),a.uniform2f(x,t.width,t.height),a.uniform1f(b,e),a.uniform1f(w,G.progress),a.uniform1f(T,G.alpha),a.uniform1f(E,X),a.uniform2f(R,G.anchor[0],G.anchor[1]),a.uniform1f(U,G.bulgeRadius),a.uniform1f(k,G.elevation),a.uniform1f(L,G.iridescence),a.uniform1f(M,G.refractStrength),a.uniform1f(_,G.travelMode),a.uniform3f(V,G.palette.a[0],G.palette.a[1],G.palette.a[2]),a.uniform3f(j,G.palette.b[0],G.palette.b[1],G.palette.b[2]),a.uniform3f(z,G.palette.c[0],G.palette.c[1],G.palette.c[2]),a.uniform3f(O,G.palette.d[0],G.palette.d[1],G.palette.d[2]),G.useTexture>0&&G.textureSource){H||(H=a.createTexture(),a.bindTexture(a.TEXTURE_2D,H),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_WRAP_S,a.CLAMP_TO_EDGE),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_WRAP_T,a.CLAMP_TO_EDGE),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_MIN_FILTER,a.LINEAR),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_MAG_FILTER,a.LINEAR)),a.activeTexture(a.TEXTURE0),a.bindTexture(a.TEXTURE_2D,H);try{a.pixelStorei(a.UNPACK_PREMULTIPLY_ALPHA_WEBGL,!1),a.pixelStorei(a.UNPACK_FLIP_Y_WEBGL,!1),a.texImage2D(a.TEXTURE_2D,0,a.RGBA,a.RGBA,a.UNSIGNED_BYTE,G.textureSource)}catch{}a.uniform1i(B,0),a.uniform1f(C,1)}else a.uniform1f(C,0);if(G.snapshotSource){W||(W=a.createTexture(),a.bindTexture(a.TEXTURE_2D,W),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_WRAP_S,a.CLAMP_TO_EDGE),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_WRAP_T,a.CLAMP_TO_EDGE),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_MIN_FILTER,a.LINEAR),a.texParameteri(a.TEXTURE_2D,a.TEXTURE_MAG_FILTER,a.LINEAR)),a.activeTexture(a.TEXTURE1),a.bindTexture(a.TEXTURE_2D,W);try{a.pixelStorei(a.UNPACK_PREMULTIPLY_ALPHA_WEBGL,!1),a.pixelStorei(a.UNPACK_FLIP_Y_WEBGL,!1),a.texImage2D(a.TEXTURE_2D,0,a.RGBA,a.RGBA,a.UNSIGNED_BYTE,G.snapshotSource)}catch{}a.uniform1i(D,1),a.uniform1f(N,1)}else a.uniform1f(N,0);a.drawElements(a.TRIANGLES,d.length,a.UNSIGNED_SHORT,0),K=requestAnimationFrame(J)};return K=requestAnimationFrame(J),{canvas:t,setProgress:e=>{G.progress=e},setAlpha:e=>{G.alpha=e},setPalette:e=>{G.palette=e},setBandTight:e=>{G.bandTight=e},setDirection:e=>{G.direction=e},setWaveAmount:e=>{G.waveAmount=e},setRippleAmount:e=>{G.rippleAmount=e},setWaveSpeed:e=>{G.waveSpeed=e},setBrightness:e=>{G.brightness=e},setSwellAmount:e=>{G.swellAmount=e},setAnchor:(e,t)=>{G.anchor=[e,t]},setBulgeRadius:e=>{G.bulgeRadius=e},setElevation:e=>{G.elevation=e},setIridescence:e=>{G.iridescence=e},setRefractStrength:e=>{G.refractStrength=e},setTravelMode:e=>{G.travelMode=e},setTextureSource:e=>{G.textureSource=e},setUseTexture:e=>{G.useTexture=e},setSnapshotSource:e=>{G.snapshotSource=e},getProgress:()=>G.progress,getAlpha:()=>G.alpha,destroy:()=>{cancelAnimationFrame(K),Y.disconnect(),a.deleteProgram(n),a.deleteBuffer(h),a.deleteBuffer(p),a.deleteBuffer(v),H&&a.deleteTexture(H),W&&a.deleteTexture(W),a.deleteShader(o),a.deleteShader(i)}}},"createShader",0,function(e={}){if("u"<typeof window)return null;let t=e.canvas??document.createElement("canvas"),a=t.getContext("webgl",{alpha:!0,antialias:!1,premultipliedAlpha:!0});if(!a)return null;let r=(e,t)=>{let r=a.createShader(e);return r?(a.shaderSource(r,t),a.compileShader(r),a.getShaderParameter(r,a.COMPILE_STATUS))?r:(console.error("[glimm] shader compile error:",a.getShaderInfoLog(r)),a.deleteShader(r),null):null},o=r(a.VERTEX_SHADER,g);if(!o)return null;let i=r(a.FRAGMENT_SHADER,v);if(!i)return a.deleteShader(o),null;let n=a.createProgram();if(!n)return a.deleteShader(o),a.deleteShader(i),null;if(a.attachShader(n,o),a.attachShader(n,i),a.linkProgram(n),!a.getProgramParameter(n,a.LINK_STATUS))return console.error("[glimm] shader link error:",a.getProgramInfoLog(n)),a.deleteProgram(n),a.deleteShader(o),a.deleteShader(i),null;a.useProgram(n);let s=a.createBuffer();a.bindBuffer(a.ARRAY_BUFFER,s),a.bufferData(a.ARRAY_BUFFER,new Float32Array([-1,-1,1,-1,-1,1,1,1]),a.STATIC_DRAW);let l=a.getAttribLocation(n,"a");a.enableVertexAttribArray(l),a.vertexAttribPointer(l,2,a.FLOAT,!1,0,0);let u=a.getUniformLocation(n,"uRes"),c=a.getUniformLocation(n,"uTime"),d=a.getUniformLocation(n,"uProgress"),h=a.getUniformLocation(n,"uAlpha"),f=a.getUniformLocation(n,"uBandTight"),p=a.getUniformLocation(n,"uPosStart"),x=a.getUniformLocation(n,"uPosEnd"),b=a.getUniformLocation(n,"uHueShift"),w=a.getUniformLocation(n,"uDirection"),A=a.getUniformLocation(n,"uWaveAmount"),y=a.getUniformLocation(n,"uRippleAmount"),T=a.getUniformLocation(n,"uWaveSpeed"),E=a.getUniformLocation(n,"uBrightness"),S=a.getUniformLocation(n,"uSwellAmount"),P=a.getUniformLocation(n,"uPalA"),R=a.getUniformLocation(n,"uPalB"),U=a.getUniformLocation(n,"uPalC"),k=a.getUniformLocation(n,"uPalD");a.enable(a.BLEND),a.blendFunc(a.ONE,a.ONE_MINUS_SRC_ALPHA);let L=(e,t,a)=>Math.max(t,Math.min(a,e)),M={progress:0,alpha:0,palette:e.palette??m(void 0),bandTight:L(e.bandTight??14,.1,200),direction:e.direction??"ltr",waveAmount:L(e.waveAmount??0,0,2),rippleAmount:L(e.rippleAmount??1,0,2),waveSpeed:L(e.waveSpeed??1,0,3),brightness:L(e.brightness??1,0,1.5),swellAmount:L(e.swellAmount??.55,0,1)},_=.4*Math.random(),F=()=>{let e=Math.min(window.devicePixelRatio||1,2),r=t.getBoundingClientRect(),o=Math.max(1,Math.round(r.width*e)),i=Math.max(1,Math.round(r.height*e));(t.width!==o||t.height!==i)&&(t.width=o,t.height=i,a.viewport(0,0,o,i))};F();let B=new ResizeObserver(F);B.observe(t);let C=performance.now(),D=0,N=()=>{let e=(performance.now()-C)/1e3,r=(e=>{switch(e){case"ltr":return{axis:0,posStart:-.2,posEnd:1.2};case"rtl":return{axis:0,posStart:1.2,posEnd:-.2};case"ttb":return{axis:1,posStart:-.2,posEnd:1.2};case"btt":return{axis:1,posStart:1.2,posEnd:-.2}}})(M.direction);a.clearColor(0,0,0,0),a.clear(a.COLOR_BUFFER_BIT),a.uniform2f(u,t.width,t.height),a.uniform1f(c,e),a.uniform1f(d,M.progress),a.uniform1f(h,M.alpha),a.uniform1f(f,M.bandTight),a.uniform1f(p,r.posStart),a.uniform1f(x,r.posEnd),a.uniform1f(w,r.axis),a.uniform1f(A,M.waveAmount),a.uniform1f(y,M.rippleAmount),a.uniform1f(T,M.waveSpeed),a.uniform1f(E,M.brightness),a.uniform1f(S,M.swellAmount),a.uniform1f(b,_),a.uniform3f(P,M.palette.a[0],M.palette.a[1],M.palette.a[2]),a.uniform3f(R,M.palette.b[0],M.palette.b[1],M.palette.b[2]),a.uniform3f(U,M.palette.c[0],M.palette.c[1],M.palette.c[2]),a.uniform3f(k,M.palette.d[0],M.palette.d[1],M.palette.d[2]),a.drawArrays(a.TRIANGLE_STRIP,0,4),D=requestAnimationFrame(N)};return D=requestAnimationFrame(N),{canvas:t,setProgress:e=>{M.progress=L(e,0,1)},setAlpha:e=>{M.alpha=L(e,0,1.5)},setPalette:e=>{M.palette=e},setBandTight:e=>{M.bandTight=L(e,.1,200)},setDirection:e=>{M.direction=e},setWaveAmount:e=>{M.waveAmount=L(e,0,2)},setRippleAmount:e=>{M.rippleAmount=L(e,0,2)},setWaveSpeed:e=>{M.waveSpeed=L(e,0,3)},setBrightness:e=>{M.brightness=L(e,0,1.5)},setSwellAmount:e=>{M.swellAmount=L(e,0,1)},getProgress:()=>M.progress,getAlpha:()=>M.alpha,destroy:()=>{cancelAnimationFrame(D),B.disconnect(),a.deleteProgram(n),a.deleteBuffer(s),a.deleteShader(o),a.deleteShader(i)}}},"cubicBezier",0,T,"hexToRgb",0,i,"oklchToRgb",0,o,"playSweep",0,function(e,t={}){let a,r,o=t.sweepMs??1100,i=t.outroMs??700,n=Math.max(0,Math.min(1,t.midpoint??.56)),s=S(t.easing??"ease")??P,l=Math.max(0,Math.min(1.5,t.peakAlpha??1));t.palette&&e.setPalette(m(t.palette)),void 0!==t.bandTight&&e.setBandTight(t.bandTight),t.direction&&e.setDirection(t.direction),void 0!==t.waveAmount&&e.setWaveAmount(t.waveAmount),void 0!==t.rippleAmount&&e.setRippleAmount(t.rippleAmount),void 0!==t.waveSpeed&&e.setWaveSpeed(t.waveSpeed),void 0!==t.brightness&&e.setBrightness(t.brightness),void 0!==t.swellAmount&&e.setSwellAmount(t.swellAmount);let u=!1,c=0,d=new Promise(e=>{a=e}),h=new Promise(e=>{r=e});return(async()=>{var d,h,f,p,m,g;let v=e.getProgress(),x=v>=.999?0:Math.max(0,Math.min(1,v));e.setAlpha(l),e.setProgress(x);let b=1-x,w=Math.max(80,o*b),A=!1,y=Promise.resolve(),T=()=>{A=!0,(y=Promise.resolve().then(()=>t.onMidpoint?.()).then(()=>{})).then(a,e=>{console.error("[glimm] midpoint callback failed:",e),a()})},E=performance.now();await new Promise(t=>{let a=()=>{if(u)return void t();let r=Math.min(1,(performance.now()-E)/w),o=x+b*s(r);e.setProgress(o),!A&&o>=n&&T(),r<1?c=requestAnimationFrame(a):t()};c=requestAnimationFrame(a)}),u||(A||T(),await y.catch(()=>{}),u||(await (d=i,h=l,f=R,p=e.setAlpha,m=()=>u,g=e=>{c=e},new Promise(e=>{let t=performance.now(),a=()=>{if(m())return void e();let r=Math.min(1,(performance.now()-t)/d);p(h+(0-h)*f(r)),r<1?g(requestAnimationFrame(a)):e()};g(requestAnimationFrame(a))})),u||(e.setProgress(0),t.onComplete?.(),r())))})(),{midpoint:d,done:h,cancel:()=>{u=!0,cancelAnimationFrame(c),a(),r()}}},"resolveEasing",0,S,"resolvePalette",0,m,"rgbToOklch",0,r,"shuffleAccentPalette",0,function(){let e=Math.random(),t=e<.3?2:e<.7?3:e<.9?4:5,a=u.filter(e=>"brown"!==e),r=a[Math.floor(Math.random()*a.length)],o=.5>Math.random()?1:-1,[i,n]=.7>Math.random()?[25,65]:[70,150],s=new Set([r]),d=[r];for(let e=1;e<t;e++){let e=function(e,t,a,r,o){let i=u.filter(a=>"brown"!==a&&!t.has(a)&&a!==e),n=u.indexOf(e),s=i.map(t=>{let r,o=u.indexOf(t),i=1===a?(o-n+u.length)%u.length:(n-o+u.length)%u.length,s=(r=180*c[e].H/Math.PI,Math.abs((180*c[t].H/Math.PI-r+540)%360-180));return{name:t,ringStep:i,dHue:s}}).filter(e=>e.dHue>=r&&e.dHue<=o).sort((e,t)=>e.ringStep-t.ringStep);return s.length>0?s[0].name:i[0]??e}(d[d.length-1],s,o,i,n);d.push(e),s.add(e)}return f(d.map(e=>l[e]))}])}]);